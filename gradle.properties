org.gradle.jvmargs=-Xmx3g -XX:MaxMetaspaceSize=1g
org.gradle.caching=true
org.gradle.configuration-cache=true
org.gradle.parallel=true

kotlin.code.style=official

# Тесты linuxX64 на macOS не запускаются, и KGP сам выключает задачу `linuxX64Test` на не-Linux
# хосте — предупреждать об этом на каждой конфигурации незачем. Закрывающий прогон вехи идёт в CI
# на Linux, где задача включена. См. docs/research/research-architecture.md, Р9.
kotlin.native.ignoreDisabledTargets=true
